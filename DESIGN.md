# MSN 会议论文初步方案：面向移动边缘网络的图感知大模型推理调度

## 1. 初步考虑

结合两篇参考工作和 MSN 的会议方向，我目前更倾向于研究**移动边缘环境中的大模型协同推理调度**。

第一篇参考工作主要研究边缘节点上的容器版本更新，通过镜像层共享、更新顺序控制和任务调度降低更新时间与服务中断。这个方向更偏向边缘系统运维，如果继续推进，需要在真实系统、灰度更新和失败回滚等方面形成比较明确的新问题。

第二篇参考工作研究移动边缘环境中的分布式大模型推理，将方法分为请求合批和 Transformer 计算块调度两个部分，并考虑用户移动引起的基站切换和链路变化。这个场景与 MSN 的移动网络主题更贴合，也便于后续向 TMC 扩展。

因此，我计划保留“动态合批 + 连续计算块组调度”的总体框架，但不直接照搬参考论文的求解方法。初步思路是：

> 将边缘—云计算基础设施建模为时变图，利用边属性感知的图注意力网络提取拓扑、链路和节点资源状态；针对同一服务锚点下的等待请求，在候选执行节点约束下使用滚动动态规划形成预填充批次；再由单智能体竞争式双重深度 Q 网络选择目标节点和连续计算块组长度。

这一方案只在计算块组调度部分使用强化学习，合批部分采用可解释的动态规划，避免两个子问题都使用多智能体学习。同时，本文不在线迁移模型参数，而是在预先部署的模型块副本之间调度请求，因此研究对象更准确地称为**计算块组执行调度**，而不是模型放置。

---

## 2. 研究场景

考虑一个由移动终端、基站边缘服务器和远程云组成的移动边缘大模型服务系统。

移动终端包括智能手机、网联车辆和无人机等。终端负责：

- 产生大模型推理请求；
- 通过当前接入基站上传输入；
- 接收最终推理结果；
- 提供位置、速度、移动方向和接入基站等移动状态。

移动终端不参与多用户批次的 Transformer 计算。真正的候选计算节点只包括边缘服务器和云服务器：

$$
\mathcal{V}_{t}^{\mathrm{comp}}
=
\mathcal{S}_{t}^{\mathrm{edge}}
\cup
\mathcal{S}^{\mathrm{cloud}}.
$$

这一设定可以避免一个多用户批次被调度到某个用户终端后无法解释的问题。边缘服务器与基站共址或通过低时延链路连接，负责请求合批、模型计算块执行、键值缓存保存和解码；云服务器部署完整模型，并作为边缘资源不足时的回退节点。

### 2.1 模型块预部署

大模型被划分为 $L$ 个连续的 Transformer 计算块。会议版不考虑在线传输和迁移模型参数，而是假设每个边缘节点预先部署一部分可执行的连续计算块，云节点部署完整模型。

定义可用矩阵

$$
c_{v,\ell}\in\{0,1\},
$$

其中，$c_{v,\ell}=1$ 表示节点 $v$ 已经部署并能够执行第 $\ell$ 个计算块。

节点 $v$ 只有在满足

$$
c_{v,\ell}=1,
\qquad
\forall \ell\in[l,l+g-1]
$$

时，才能执行从第 $l$ 个计算块开始、长度为 $g$ 的连续计算块组。

因此，本文优化的是请求批次在已有模型块副本之间的执行调度，不涉及模型权重下载、块缓存淘汰和在线模型迁移。这些内容可以作为后续 TMC 扩展中的长时间尺度问题。

### 2.2 服务锚点与队列

每个移动用户通过当前接入基站访问推理服务，该基站被定义为请求的服务锚点。每个服务锚点维护自己的预填充等待队列：

$$
\mathcal{Q}_{b,t},
$$

其中，$b$ 表示基站或与其共址的边缘节点。

会议版限定一个批次只能从同一服务锚点的队列中形成：

$$
\mathcal{B}_{t}
\subseteq
\mathcal{Q}_{b,t}.
$$

同一批次中的请求还需要满足：

- 使用同一个大模型；
- 处于相同推理阶段；
- 输入长度位于兼容区间；
- 精度和服务配置一致。

中心编排器可以观察全局边缘—云资源，但每次只为某一个服务锚点的队列形成批次。这样无需处理跨基站输入汇聚、多个锚点同步和多路径回传等额外问题。

---

## 3. 推理流程与优化范围

为了控制会议版的研究范围，本文明确区分预填充阶段和解码阶段。

### 3.1 预填充阶段

1. 新请求进入当前服务锚点对应的预填充等待队列；
2. 调度器从同一队列中选择兼容请求形成批次；
3. 对该批次依次决定连续计算块组的执行节点；
4. 各节点依次完成全部 Transformer 计算块的预填充计算；
5. 完成预填充后，为每个请求得到一条确定的计算块执行映射。

设请求 $r_i$ 的执行映射为

$$
\Pi_i
=
\left\{
(v_1,g_1),
(v_2,g_2),
\ldots,
(v_J,g_J)
\right\},
$$

其中，

$$
\sum_{j=1}^{J}g_j=L.
$$

同一个预填充批次中的请求在预填充阶段共享相同的计算块执行映射。

### 3.2 解码阶段

预填充完成后，固定的是每个请求的计算块执行映射，而不是请求之间的批次成员关系。

解码阶段允许执行节点使用现有的连续批处理机制：

- 已完成生成的请求可以退出解码批次；
- 新请求只有在模型、服务配置和计算块映射一致时，才能加入相同服务路径上的连续批处理；
- 请求的计算块映射保持不变；
- 请求对应的键值缓存所在节点保持不变；
- 不为了重新合批而迁移键值缓存或改变模型路径。

因此，会议版只在预填充阶段联合决定请求批次与计算块执行映射，但该决策必须考虑后续整个解码阶段的预期成本。

---

## 4. 移动性如何影响推理调度

用户移动不会直接改变已经部署在边缘和云节点上的模型参数，但会改变用户的服务锚点和访问路径。

本文采用“服务锚点跟随接入基站”的机制。请求开始时由当前接入基站作为服务锚点；如果用户在推理过程中切换到新基站，计算块执行映射和键值缓存位置保持不变，新基站需要通过边缘回传网络继续访问原来的执行路径。

因此，基站切换会带来：

1. 切换和服务重路由开销；
2. 新基站访问原执行路径产生的路径拉长开销；
3. 最终结果从原执行节点返回到新基站的额外时延。

请求 $r_i$ 的移动切换开销表示为

$$
C_i^{\mathrm{ho}}
=
T_i^{\mathrm{reroute}}
+
T_i^{\mathrm{stretch}},
$$

其中，$T_i^{\mathrm{reroute}}$ 表示重新建立服务路径的开销，$T_i^{\mathrm{stretch}}$ 表示新锚点访问原计算路径产生的额外传输时延。

根据用户最近的位置、速度、方向和接入历史，系统轻量估计：

- 当前基站的剩余驻留时间；
- 预计推理窗口内发生切换的概率；
- 下一候选基站；
- 切换后的回传路径长度。

会议版可以采用速度方向外推、历史平均驻留时间或一阶基站转移概率，不额外引入复杂的轨迹预测模型。

---

## 5. 基础设施时变图

### 5.1 图结构

不将所有活跃移动用户都作为长期图节点，而是只构建计算和通信基础设施图：

$$
\mathcal{G}_{t}^{\mathrm{infra}}
=
\left(
\mathcal{V}_{t}^{\mathrm{comp}},
\mathcal{E}_{t}^{\mathrm{backhaul}}
\right),
$$

其中，$\mathcal{V}_{t}^{\mathrm{comp}}$ 包括边缘服务器和云服务器，$\mathcal{E}_{t}^{\mathrm{backhaul}}$ 表示边缘之间以及边缘—云之间的可用链路。

这样可以避免活跃用户数量变化导致图规模剧烈波动，也不会让大量与当前批次无关的用户给图消息传递带来噪声。

### 5.2 节点特征

对于计算节点 $v$，节点特征表示为

$$
\mathbf{x}_{v}^{t}
=
\left[
F_v^t,
M_v^t,
Q_v^t,
K_v^t,
\mathbf{c}_v,
\chi_v
\right],
$$

其中：

- $F_v^t$ 表示当前可用计算能力；
- $M_v^t$ 表示剩余显存；
- $Q_v^t$ 表示节点排队状态；
- $K_v^t$ 表示键值缓存占用；
- $\mathbf{c}_v=[c_{v,1},\ldots,c_{v,L}]$ 表示节点已经部署的模型计算块；
- $\chi_v$ 表示边缘或云节点类型。

### 5.3 链路特征

对于基础设施链路 $(u,v)$，边特征表示为

$$
\mathbf{e}_{u,v}^{t}
=
\left[
B_{u,v}^{t},
D_{u,v}^{t},
P_{u,v}^{t}
\right],
$$

其中：

- $B_{u,v}^{t}$ 表示可用带宽；
- $D_{u,v}^{t}$ 表示传输时延；
- $P_{u,v}^{t}$ 表示链路可靠性或丢包特征。

### 5.4 当前批次与移动状态表示

当前服务锚点、请求特征和移动状态不作为长期图节点，而是编码为批次向量：

$$
\mathbf{z}_{\mathcal{B}}^{t}
=
\left[
|\mathcal{B}|,
l_{\max}^{\mathrm{in}},
l_{\mathrm{sum}}^{\mathrm{in}},
\hat l_{\mathrm{avg}}^{\mathrm{out}},
d_{\min}^{\mathrm{rem}},
b^{\mathrm{anchor}},
\hat b^{\mathrm{next}},
p^{\mathrm{ho}},
\tau^{\mathrm{res}}
\right].
$$

其中：

- $|\mathcal{B}|$ 为批次大小；
- $l_{\max}^{\mathrm{in}}$ 为最大输入长度；
- $l_{\mathrm{sum}}^{\mathrm{in}}$ 为批次总输入令牌数；
- $\hat l_{\mathrm{avg}}^{\mathrm{out}}$ 为预计平均输出长度；
- $d_{\min}^{\mathrm{rem}}$ 为批次中最紧迫请求的剩余时延预算；
- $b^{\mathrm{anchor}}$ 为当前服务锚点；
- $\hat b^{\mathrm{next}}$ 为预测下一基站；
- $p^{\mathrm{ho}}$ 为预计切换概率；
- $\tau^{\mathrm{res}}$ 为预计剩余驻留时间。

必要时，也可以临时构造一个虚拟批次节点，并将其连接到当前服务锚点和预测下一基站，但会议版优先采用批次向量，以控制图规模和实现复杂度。

---

## 6. 请求模型与总优化目标

请求 $r_i$ 表示为

$$
r_i
=
\left(
a_i,
l_i^{\mathrm{in}},
\hat l_i^{\mathrm{out}},
d_i,
m_i,
b_i^t,
\tau_i^{\mathrm{res}}
\right),
$$

其中：

- $a_i$ 为请求到达时间；
- $l_i^{\mathrm{in}}$ 为输入长度；
- $\hat l_i^{\mathrm{out}}$ 为预计输出长度；
- $d_i$ 为端到端时延约束；
- $m_i$ 为模型类型；
- $b_i^t$ 为当前接入基站；
- $\tau_i^{\mathrm{res}}$ 为预计剩余驻留时间。

请求的剩余时延预算为

$$
d_i^{\mathrm{rem}}(t)
=
d_i-(t-a_i).
$$

请求总时延明确拆分为

$$
T_i
=
T_i^{\mathrm{queue}}
+
T_i^{\mathrm{prefill}}
+
T_i^{\mathrm{decode}}
+
T_i^{\mathrm{handover}}.
$$

整体目标是在资源约束下最小化：

$$
\min
\sum_i
\left(
\alpha T_i
+
\beta \mathbb{I}(T_i>d_i)
+
\gamma C_i^{\mathrm{trans}}
+
\eta C_i^{\mathrm{ho}}
\right),
$$

其中，$C_i^{\mathrm{trans}}$ 表示预填充和解码过程中跨节点传输中间特征产生的总开销。

---

## 7. 完整方法流程

整体方法按照以下流程运行：

```text
当前基础设施拓扑、链路和资源状态
                    ↓
       构建边缘—云基础设施时变图
                    ↓
      边属性感知图注意力网络编码
                    ↓
     硬过滤不可行节点和计算块组
                    ↓
保留当前锚点、时延下界最小的前 N 个节点以及云节点
                    ↓
针对每个候选“节点—计算块组”运行滚动动态规划
                    ↓
生成候选“节点—请求批次—计算块组”组合
                    ↓
共享竞争式双重深度 Q 网络统一评分
                    ↓
选择当前批次、首个执行节点和连续计算块数量
                    ↓
对固定批次继续选择后续节点与计算块组
                    ↓
完成预填充映射，并在解码阶段保持映射和 KV Cache 位置不变
```

在首次决策中，动态规划和强化学习共同选择请求批次、首个执行节点和首个计算块组。批次形成后，本次预填充中的请求成员保持不变；后续只继续决定剩余连续计算块组的执行节点。

---

## 8. 候选节点与完整路径可行性

### 8.1 硬过滤

首先过滤明显不可行的节点：

$$
\mathcal{V}_{t}^{\mathrm{feas}}(l,g)
=
\left\{
v:
\begin{array}{l}
v\text{ 可达},\\
c_{v,\ell}=1,\ \forall\ell\in[l,l+g-1],\\
M_v^t\geq M_{\min}(l,g)
\end{array}
\right\}.
$$

### 8.2 基于时延下界的候选筛选

不额外训练候选节点评分网络，而是根据单步时延下界排序：

$$
\underline{T}(v,g)
=
T_v^{\mathrm{queue}}
+
T^{\mathrm{input}}(b^{\mathrm{anchor}},v)
+
T_v^{\mathrm{profile}}(g).
$$

候选集合定义为

$$
\mathcal{V}_{t}^{\mathrm{cand}}(l,g)
=
\left(
\left\{
v^{\mathrm{anchor}}
\right\}
\cap
\mathcal{V}_{t}^{\mathrm{feas}}(l,g)
\right)
\cup
\operatorname{TopN}
\left(
\mathcal{V}_{t}^{\mathrm{feas}}(l,g),
\underline{T}(v,g)
\right)
\cup
\left\{
v^{\mathrm{cloud}}
\right\}.
$$

当锚点边缘节点满足目标计算块可用、显存充足和链路可达等约束时，优先将其保留在候选集合中；锚点节点不能绕过硬过滤直接参与调度。云节点部署完整模型，作为后续完整执行路径的候选回退节点，但批次是否允许发射还需要满足云端安全容量约束。

### 8.3 完整路径可行性

只判断首个计算块组可行是不够的。定义

$$
\Phi(\mathcal{B},l)=1
$$

表示从当前计算块 $l$ 开始，至少存在一条能够完成剩余模型的可行执行路径。

会议版不求解完整最优路径，而采用“云端回退 + 安全容量预留 + 延迟发射”的方式保证可行性：

1. 云节点部署完整模型，并始终作为候选回退节点；
2. 云端为边缘回退保留安全容量 $M_{\mathrm{cloud}}^{\mathrm{reserve}}$；
3. 调度器只发射满足云端保底容量约束的批次；
4. 如果后续边缘节点均不可行，则将剩余计算块回退到云节点执行；
5. 如果当前没有满足约束的合法批次，则缩小批次；仍不可行时，将请求继续保留在等待队列，而不是强行发射。

批次形成时需要满足

$$
M_{\mathrm{cloud}}(\mathcal{B})
\leq
M_{\mathrm{cloud}}^{t}
-
M_{\mathrm{cloud}}^{\mathrm{reserve}}.
$$

如果当前所有候选组合均不满足资源和时延约束，则令

$$
\mathcal{B}_{t}=\varnothing,
$$

表示当前调度时刻暂不发射新批次。通过在发射前检查云端保底容量，已经发射的批次在后续边缘路径失效时仍至少保留一条云端完成路径。

---

## 9. 节点条件下的滚动动态规划合批

### 9.1 候选请求窗口

对于服务锚点 $b$，每次只考虑其队首前 $K$ 个请求：

$$
\mathcal{Q}_{b,t}^{K}
=
\{r_1,r_2,\ldots,r_K\}.
$$

先筛除模型类型、推理阶段和服务配置不一致的请求，再按输入长度划分兼容区间。

### 9.2 批次效用

对于候选首节点 $v$、首个连续计算块组长度 $g$、目标批大小 $b$ 和最大输入长度桶 $\bar l$，定义批次效用：

$$
U(\mathcal{B}\mid v,g)
=
\alpha_1
\sum_{i\in\mathcal{B}}
\frac{w_i}{d_i^{\mathrm{rem}}}
+
\alpha_2
G^{\mathrm{batch}}(\mathcal{B};v,g)
-
\alpha_3
C^{\mathrm{pad}}(\mathcal{B})
-
\alpha_4
C^{\mathrm{mob}}(\mathcal{B};v,g).
$$

填充开销为

$$
C^{\mathrm{pad}}(\mathcal{B})
=
|\mathcal{B}|
\max_{i\in\mathcal{B}}l_i^{\mathrm{in}}
-
\sum_{i\in\mathcal{B}}l_i^{\mathrm{in}}.
$$

批处理收益由本地测量得到：

$$
G^{\mathrm{batch}}(\mathcal{B};v,g)
=
\sum_{i\in\mathcal{B}}
\widehat{T}_{i}^{\mathrm{single}}(v,g)
-
\widehat{T}^{\mathrm{batch}}(\mathcal{B};v,g).
$$

移动风险项为

$$
C^{\mathrm{mob}}(\mathcal{B};v,g)
=
\sum_{i\in\mathcal{B}}
p_i^{\mathrm{ho}}
\left(
\widehat{T}^{\mathrm{batch}}(\mathcal{B};v,g)
\right)
C_i^{\mathrm{ho}}(v).
$$

### 9.3 动态规划状态与滚动递推

固定候选节点 $v$、计算块组长度 $g$、最大输入长度桶 $\bar l$ 和目标批大小 $b$。只保留满足

$$
l_i^{\mathrm{in}}\leq\bar l
$$

的候选请求，并将输入令牌数离散化为 $\tilde l_i$。

从完整定义看，可以先写出三维状态

$$
\mathrm{DP}[j,k,s],
$$

表示考虑前 $j$ 个请求、已经选择 $k$ 个请求、总离散输入令牌数为 $s$ 时，能够获得的最大可加请求价值。由于第 $j$ 个请求的状态只依赖上一轮结果，实际实现时在请求维度上采用滚动更新，将其压缩为二维状态

$$
\mathrm{DP}[k,s].
$$

在 $v$、$g$、$\bar l$ 和 $b$ 固定后，请求 $r_i$ 的可加价值定义为

$$
u_i(v,g,\bar l,b)
=
\alpha_1
\frac{w_i}{d_i^{\mathrm{rem}}}
-
\alpha_3
(\bar l-l_i^{\mathrm{in}})
-
\alpha_4
p_i^{\mathrm{ho}}
\left(
\widehat{T}^{\mathrm{batch}}(b,\bar l;v,g)
\right)
C_i^{\mathrm{ho}}(v).
$$

遍历请求 $r_j$ 时，按照 $k$ 和 $s$ 的递减顺序更新：

$$
\mathrm{DP}[k,s]
=
\max
\left\{
\mathrm{DP}[k,s],
\mathrm{DP}[k-1,s-\tilde l_j]
+
u_j(v,g,\bar l,b)
\right\}.
$$

反向更新可以避免同一个请求在一轮中被重复选择。初始化为

$$
\mathrm{DP}[0,0]=0,
$$

其他不可达状态取负无穷。

当选中 $b$ 个请求后，将节点相关的批处理收益作为批次级常数加入：

$$
U
=
\mathrm{DP}[b,s]
+
\alpha_2
G^{\mathrm{batch}}(b,\bar l;v,g).
$$

然后在所有合法的 $s$、$\bar l$、$b$、$v$ 和 $g$ 中选择效用最大的候选批次。因此，算法在实现上是以“已选请求数”和“总离散令牌数”为状态维度的二维滚动动态规划。

### 9.4 可行性约束

候选批次需要满足：

$$
\sum_{i\in\mathcal{B}}l_i^{\mathrm{in}}
\leq
C_{\mathrm{token}},
$$

$$
M(\mathcal{B},v,g)
\leq
M_v^t,
$$

$$
M_{\mathrm{cloud}}(\mathcal{B})
\leq
M_{\mathrm{cloud}}^t
-
M_{\mathrm{cloud}}^{\mathrm{reserve}},
$$

以及基于云端回退路径估计的剩余时延约束：

$$
\widehat{T}_{i}^{\mathrm{first}}(\mathcal{B},v,g)
+
\widehat{T}_{i}^{\mathrm{cloud\mbox{-}fallback}}
\leq
d_i^{\mathrm{rem}}
+
\epsilon,
\qquad
\forall i\in\mathcal{B}.
$$

这样既保证首个节点可执行，也保证已经发射的批次后续至少存在一条云端回退路径。如果没有任何候选批次满足上述约束，则令 $\mathcal{B}_t=\varnothing$，请求继续等待下一次调度。

---

## 10. 边属性感知的图注意力网络

对于基础设施图中的相邻节点 $u$ 和 $v$，注意力得分定义为

$$
s_{u,v}^{t}
=
\operatorname{LeakyReLU}
\left(
\mathbf{a}^{\mathsf T}
\left[
W_n\mathbf{h}_u^t
\Vert
W_n\mathbf{h}_v^t
\Vert
W_e\mathbf{e}_{u,v}^t
\right]
\right).
$$

归一化后得到

$$
\alpha_{u,v}^{t}
=
\frac{
\exp(s_{u,v}^{t})
}{
\sum_{k\in\mathcal{N}(u)}
\exp(s_{u,k}^{t})
}.
$$

节点的新表示为

$$
\mathbf{h}_{u}^{t\prime}
=
\sigma
\left(
\sum_{v\in\mathcal{N}(u)}
\alpha_{u,v}^{t}
W_n\mathbf{h}_v^t
\right).
$$

网络级表示由所有计算节点经过池化得到：

$$
\mathbf{h}_{\mathrm{G}}^{t}
=
\operatorname{Pool}
\left(
\{
\mathbf{h}_{v}^{t\prime}
:
v\in\mathcal{V}_{t}^{\mathrm{comp}}
\}
\right).
$$

图注意力网络只负责提取动态拓扑和资源状态，不负责候选节点筛选。候选筛选由硬约束和时延下界完成。

---

## 11. 共享竞争式双重深度 Q 网络

### 11.1 动作定义

在新批次发射阶段，动作表示为

$$
a_t
=
\left(
v,
g,
\mathcal{B}_{t}^{v,g}
\right),
$$

其中，$\mathcal{B}_{t}^{v,g}$ 是动态规划针对候选节点 $v$ 和计算块组长度 $g$ 形成的候选批次。

批次形成后的后续步骤中，动作简化为

$$
a_t=(v,g).
$$

### 11.2 可变动作空间

不使用固定维度输出层，而是采用节点级参数共享的 Q 值评分函数：

$$
Q_{\theta}(v,g,\mathcal{B}\mid s_t)
=
f_{\theta}
\left(
\mathbf{h}_{v}^{t},
\mathbf{h}_{\mathrm{G}}^{t},
\mathbf{z}_{\mathcal{B}}^{t},
\mathbf{z}_{g},
\mathbf{z}_{l}
\right),
$$

其中：

- $\mathbf{h}_{v}^{t}$ 为候选节点表示；
- $\mathbf{h}_{\mathrm{G}}^{t}$ 为网络级表示；
- $\mathbf{z}_{\mathcal{B}}^{t}$ 为当前批次特征；
- $\mathbf{z}_{g}$ 为计算块组长度编码；
- $\mathbf{z}_{l}$ 为当前计算块位置编码。

所有候选节点共用同一个评分网络，因此节点数量和拓扑改变时不需要修改输出层。

### 11.3 竞争式双重结构

竞争式结构将状态价值和动作优势分开：

$$
Q_{\theta}(a\mid s)
=
V_{\theta}(s)
+
A_{\theta}(s,a)
-
\frac{1}{|\mathcal{A}_t|}
\sum_{a'\in\mathcal{A}_t}
A_{\theta}(s,a').
$$

同时使用双重 Q 学习降低价值高估。图注意力网络与 Q 值评分网络端到端训练，动态规划本身不参与梯度更新。

### 11.4 动作掩码

以下动作直接设为不可选：

- 节点不包含目标连续计算块；
- 节点剩余显存不足；
- 当前执行节点与目标节点之间不可达；
- 计算块组长度超过模型剩余层数；
- 节点预计执行后必然导致时延违约；
- 节点不支持目标模型或服务配置。

对于已经通过云端保底容量检查并发射的批次，云节点始终保留且部署完整模型，因此后续计算块调度至少存在云端回退动作。对于尚未发射的请求，如果连云端保底容量也不满足，则不生成动作，而是令 $\mathcal{B}_t=\varnothing$ 并继续等待。

---

## 12. 预填充决策中的预期解码成本

虽然本文只在预填充阶段决定计算块映射，但该映射会在后续所有输出令牌的解码过程中重复使用。因此，不能只优化当前预填充计算块组的局部时延。

对于请求 $r_i$ 和预填充阶段确定的映射 $\Pi_i$，预期解码时延表示为

$$
\widehat{T}_{i}^{\mathrm{decode}}(\Pi_i)
=
\hat l_i^{\mathrm{out}}
\left[
\sum_{(v,g)\in\Pi_i}
\widehat{T}^{\mathrm{dec}}(v,g)
+
\sum_{(u,v)\in\Pi_i}
\widehat{T}_{\mathrm{trans}}^{\mathrm{dec}}(u,v)
\right].
$$

其中，$\hat l_i^{\mathrm{out}}$ 可以来自：

- 用户设置的最大输出长度；
- 根据请求类型得到的输出长度估计；
- Microsoft Azure 请求轨迹拟合的条件期望。

为避免重复计费，已经发生的预填充、传输和切换开销只通过单步奖励累计：

$$
r_t^{\mathrm{step}}
=
-\lambda_1
\Delta T_t^{\mathrm{prefill}}
-\lambda_2
\Delta C_t^{\mathrm{trans}}
-\lambda_3
\Delta C_t^{\mathrm{ho}}.
$$

其中，$\Delta T_t^{\mathrm{prefill}}$、$\Delta C_t^{\mathrm{trans}}$ 和 $\Delta C_t^{\mathrm{ho}}$ 分别表示当前动作新增的预填充时延、跨节点传输成本和移动切换成本。

当最后一个预填充计算块完成、整条映射 $\Pi_i$ 确定后，只额外加入此前无法准确评估的未来解码成本和最终时延违约风险：

$$
r_T
\leftarrow
r_T
-
\mu_1
\widehat{T}_i^{\mathrm{decode}}(\Pi_i)
-
\mu_2
\mathbb{I}
\left(
\widehat{T}_i^{\mathrm{total}}>d_i
\right),
$$

其中，

$$
\widehat{T}_i^{\mathrm{total}}
=
T_i^{\mathrm{queue}}
+
T_i^{\mathrm{prefill}}
+
\widehat{T}_i^{\mathrm{decode}}(\Pi_i)
+
T_i^{\mathrm{handover}}.
$$

对于一个批次，可以对其中所有请求的解码成本和违约项求和或取加权平均。这样，单步奖励负责已经发生的预填充成本，终止奖励只负责映射确定后才能估计的未来解码成本和最终违约风险，不会对同一项成本重复惩罚。

---

## 13. 数据使用与测量计划

不同公开数据来自不同地区、时间和生产系统，本文不将它们按时间戳拼接，也不声称它们共同构成一个真实的移动边缘大模型业务场景。

整体采用：

> 电信移动轨迹作为场景主线，本地测量校准大模型执行开销，Microsoft Azure 数据校准请求形状，资源状态采用受控生成，阿里生产集群数据仅作为可选压力测试。

### 13.1 移动场景

电信移动轨迹用于重放：

- 用户位置和移动过程；
- 基站关联关系；
- 基站驻留时间和切换序列；
- 不同时段、不同基站的活跃用户数量。

不将每条通信活动记录直接等同为一次大模型请求。设基站 $b$ 在时刻 $t$ 的活跃用户数为 $N_b^{\mathrm{active}}(t)$，则该基站的大模型请求强度可表示为

$$
\lambda_b(t)
=
\lambda_0
+
\kappa
N_b^{\mathrm{active}}(t).
$$

再依据非齐次泊松过程或伯努利抽样生成实际请求。这样，电信轨迹负责调节请求强度与移动环境，但不人为声称普通通信事件就是大模型调用。

### 13.2 请求特征

Microsoft Azure 大模型请求轨迹只用于拟合输入和输出长度分布：

$$
l_i^{\mathrm{in}}
\sim
P_{\mathrm{Azure}}^{\mathrm{in}},
\qquad
\hat l_i^{\mathrm{out}}
\sim
P_{\mathrm{Azure}}^{\mathrm{out}}
\left(
l^{\mathrm{in}},
\mathrm{type}
\right).
$$

不将 Azure 原始时间戳与电信轨迹对齐。

另外，可以在独立的跨工作负载实验中，用完整 Azure 到达序列替换主实验请求生成器。此时只评价面对未见请求模式时的泛化能力，不将其解释为与电信轨迹对应的联合现实场景。

### 13.3 节点资源

终端、边缘和云节点的资源容量按照典型硬件配置和本地测量结果设置。背景负载采用低、中、高和突发变化等受控模式。

阿里生产集群轨迹不进入主实验。若数据字段适合，可以提取归一化负载变化形状，作为可选的跨资源分布压力测试，不直接将数据中心 GPU 负载解释为真实边缘服务器负载。

### 13.4 本地大模型测量

选择规模可控的开源模型，例如 Qwen 1.5B 或 3B，测量：

- 不同输入长度和批大小下的预填充时间；
- 不同批大小和上下文长度下的单令牌解码时间；
- 不同连续计算块数量下的计算时间；
- 不同硬件上的最优批大小；
- 中间特征传输量；
- 键值缓存显存增长；
- 不同背景负载下的执行时延。

测量结果保存为查找表或拟合函数：

$$
T_{\mathrm{pre}}
=
f_{\mathrm{pre}}
(B,l^{\mathrm{in}},g,v,\rho_v),
$$

$$
T_{\mathrm{dec}}
=
f_{\mathrm{dec}}
(B,l^{\mathrm{ctx}},g,v,\rho_v).
$$

由于会议版不在线迁移模型参数，不再把模型块首次加载时间作为主要决策成本。

---

## 14. 需要先完成的三组动机测量

目前方法结构已经相对完整，下一步最重要的不是继续增加算法，而是先验证三条核心观察。

### 14.1 观察一：合批收益具有节点依赖性

对同一组请求，在不同硬件或不同背景负载节点上测量：

- 合批加速比；
- 显存占用；
- 最优批大小；
- 时延约束满足率。

希望验证：

> 同一个批次在不同执行节点上的收益和可行性明显不同，因此不能先全局合批、再独立选择节点，而需要节点条件下的合批。

### 14.2 观察二：连续计算块组长度存在权衡

令

$$
g\in\{1,2,4,8,16\},
$$

测量：

- 调度决策次数；
- 跨节点传输次数；
- 节点负载均衡程度；
- 端到端推理时延。

希望验证：

> 逐块调度会增加决策和通信开销，但计算块组也不是越长越好，因为过长块组会降低资源适配和负载均衡能力。

### 14.3 观察三：图表示有利于未知拓扑泛化

训练时使用固定范围的节点数量和连接结构，测试时改变：

- 边缘节点数量；
- 链路连接关系；
- 节点异构程度；
- 网络拥塞位置。

比较普通全连接网络和图注意力网络，希望验证：

> 固定向量表示难以适应节点数量和拓扑变化，而图表示能够通过节点级参数共享支持跨规模和跨拓扑泛化。

这三条观察分别对应节点条件合批、动态计算块组长度和图感知调度，是论文方法设计的主要动机。

---

## 15. 实验设计

### 15.1 合批基线

- 不合批；
- 固定批大小；
- 先到先服务贪心；
- 参考论文中的收益—代价贪心；
- 本文的节点条件滚动动态规划。

### 15.2 调度基线

- 全部云端执行；
- 当前锚点边缘优先；
- 固定边缘—云模型切分；
- 负载感知贪心；
- 普通全连接网络与双重深度 Q 网络；
- 图注意力网络与贪心选择；
- 本文的图注意力网络与竞争式双重深度 Q 网络。

如果时间允许，可以复现一个简化的多智能体近端策略优化方法。若无法在相同环境中复现，不应直接引用参考论文中的数值与本文结果比较。

### 15.3 主要指标

- 平均端到端时延；
- 第 95 百分位时延；
- 时延约束满足率；
- 系统吞吐量；
- 中间特征传输量；
- 平均与峰值显存占用；
- 基站切换造成的额外时延；
- 合批和调度的在线决策时间。

### 15.4 主要变量

- 请求到达率；
- 输入与输出长度；
- 长请求比例；
- 用户移动速度；
- 基站切换频率；
- 边缘节点数量；
- 节点背景负载；
- 链路带宽和拓扑；
- 模型规模；
- 可选计算块组长度集合。

### 15.5 消融实验

1. 去掉移动状态和切换成本；
2. 将图注意力网络替换为普通全连接网络；
3. 将动态计算块组长度改为固定长度；
4. 将节点条件动态规划替换为全局贪心合批；
5. 奖励中去掉预期解码成本；
6. 去掉云端完整路径兜底约束。

---

## 16. 预期贡献

目前可以将贡献概括为三点：

1. 面向移动边缘大模型服务，建立一个联合考虑同锚点请求合批、连续 Transformer 计算块组执行调度、用户切换和预期解码成本的在线优化模型。

2. 设计一个节点条件下的滚动动态规划合批方法，并提出基于边属性图注意力网络和共享竞争式双重深度 Q 网络的计算块组调度方法，在可变拓扑和可变候选动作空间下完成在线决策。

3. 构建一个由真实移动轨迹驱动、由本地大模型性能测量校准的实验平台，系统评估所提方法在动态负载、移动切换、异构资源和未知拓扑条件下的性能。

---

## 17. 会议版与后续期刊版边界

MSN 会议版暂不考虑：

- 移动终端参与大模型批次计算；
- 跨基站请求合批；
- 在线迁移模型参数；
- 模型计算块缓存与淘汰；
- 解码阶段动态改变计算块映射；
- 键值缓存迁移；
- 多模型长期部署优化；
- 强化学习收敛证明；
- 完整多节点大模型真实系统。

后续扩展到 TMC 时，可以加入：

- 计算块副本预部署和在线迁移；
- 长短时间尺度联合优化；
- 解码阶段自适应重调度；
- 键值缓存迁移与复用；
- 多模型和多租户服务；
- 跨基站协同合批；
- 小规模真实边缘—云原型系统。

---

## 18. 当前需要和老师确认的问题

1. 移动终端只负责请求产生和无线接入，不参与合批后的模型计算，这一系统范围是否合适；
2. 一个预填充批次只来自同一服务锚点，是否会把问题限制得过窄；
3. 会议版采用预部署模型块副本，只做执行调度，不在线迁移模型参数，是否符合预期；
4. 是否接受云节点作为完整路径兜底，以简化完整路径可行性约束；
5. 动态规划采用“固定节点、块组长度、最大长度桶和目标批大小”的递推方式是否合适；
6. 预填充阶段决定映射、奖励中加入预期解码成本、解码阶段固定映射的范围是否清楚；
7. 是否先完成三组动机测量，再决定最终是否保留动态图网络、动态块组长度和节点条件合批三个技术点。
